<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath />
  </parent>
  <groupId>com.leandro</groupId>
  <artifactId>stockflow-intelligence</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>stockflow-intelligence</name>
  <description>Demand forecasting service for the StockFlow ecosystem</description>
  <properties>
    <java.version>25</java.version>
    <smile.version>6.3.0</smile.version>
    <commons-statistics.version>1.3</commons-statistics.version>
    <commons-csv.version>1.14.1</commons-csv.version>
  </properties>

  <dependencies>

    <!-- REST API, embedded web server and Spring MVC -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <!-- Bean Validation for validating API requests and DTOs -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- Machine Learning algorithms for demand forecasting models -->
    <dependency>
      <groupId>com.github.haifengl</groupId>
      <artifactId>smile-core</artifactId>
      <version>${smile.version}</version>
    </dependency>

    <!-- Descriptive statistics such as mean, variance and standard deviation -->
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-statistics-descriptive</artifactId>
      <version>${commons-statistics.version}</version>
    </dependency>

    <!-- CSV parsing and generation for historical demand datasets -->
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-csv</artifactId>
      <version>${commons-csv.version}</version>
    </dependency>

    <!-- Testing support with JUnit, Mockito, AssertJ and Spring Test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <!-- Packages and runs the application as an executable Spring Boot JAR -->
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
